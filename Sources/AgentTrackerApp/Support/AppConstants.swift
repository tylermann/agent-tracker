import Foundation

enum AppConstants {
  /// Fixed size of the Settings window; the window frame and the SwiftUI content agree on it.
  static let settingsWindowSize = CGSize(width: 560, height: 640)
}
