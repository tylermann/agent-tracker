/// Identifiers shared between the app bundle and the CLI helper.
public enum AppIdentity {
  /// Bundle identifier of the Agent Tracker menu-bar app.
  public static let bundleIdentifier = "com.tyler.agenttracker"
}
